These templates must be moved to the folder containing the utility folder or they will not be able to locate the appropriate header. This is because the include line locates the header by way of a relative reference.